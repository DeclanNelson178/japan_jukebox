# Runtime
numpy>=1.22
sounddevice>=0.5      # real-time audio playback + analysis tap
soundfile>=0.14       # wav decoding (bundles libsndfile)
python-dotenv>=0.20   # HOME_PATH from .env

# Fetching new music (optional; pytube is dead, yt-dlp is the modern tool)
yt-dlp>=2024.1

# Dev
pytest>=9.0
